#!/usr/bin/env zsh

function sharpplay(){
	[ "${#${@}}" = 0 ] && echo "Please specify a title to search on youtube or an url" && return
	mpv --no-video "ytdl://ytsearch:${${$(echo -n "$@"):#"-"}:-$(xclip -o)}"
}

